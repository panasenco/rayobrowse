# GET /api/browsers

> Source: https://docs.rayobrowse.com/api-reference/local/browsers-list/

Returns a list of active browser sessions.

**URL** : `GET /api/browsers`

**Auth** : None.

## Example

Section titled “Example”

Terminal window
    
    
    curl http://localhost:9222/api/browsers

## Response

Section titled “Response”

Returns active browser sessions with `sessionId`, `wsEndpoint`, `status`, and other metadata.

[ Previous   
POST /api/browser/close ](https://docs.rayobrowse.com/api-reference/local/browser-delete/) [ Next   
GET /health ](https://docs.rayobrowse.com/api-reference/local/health/)
